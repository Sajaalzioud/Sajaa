<svg viewBox="0 0 300 300" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <filter id="shadow_mark_black">
      <feDropShadow dx="0" dy="2" stdDeviation="3" flood-opacity="0.15"/>
    </filter>
  </defs>

  <!-- Base curve (foundation) -->
  <path d="M 80 200 Q 150 160, 220 200"
        fill="none"
        stroke="#000000"
        stroke-width="10"
        stroke-linecap="round"
        filter="url(#shadow_mark_black)"
        opacity="0.9"/>

  <!-- Middle curve (growth) -->
  <path d="M 70 160 Q 150 105, 230 160"
        fill="none"
        stroke="#000000"
        stroke-width="10"
        stroke-linecap="round"
        filter="url(#shadow_mark_black)"
        opacity="0.95"/>

  <!-- Top curve (ascension) -->
  <path d="M 60 120 Q 150 60, 240 120"
        fill="none"
        stroke="#000000"
        stroke-width="10"
        stroke-linecap="round"
        filter="url(#shadow_mark_black)"
        opacity="1"/>

  <!-- Center focal point -->
  <circle cx="150" cy="130" r="3.5" fill="#000000" opacity="0.6"/>
</svg>
